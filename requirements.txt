# --- Графический интерфейс (GUI) ---
PyQt6>=6.5.0

# --- Нейросети и Компьютерное зрение (Core ML) ---
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0      # YOLOv8 для поиска судоку

# --- Обработка изображений ---
Pillow>=9.5.0           # Захват экрана и кроп изображений

# --- Автоматизация (Ввод) ---
mouse>=0.7.0            # Управление курсором и кликами
keyboard>=0.13.5        # Эмуляция нажатий клавиш

# --- Вспомогательные библиотеки (устанавливаются автоматически, но полезно знать) ---
# numpy                 # Математические операции (тянется за pytorch/ultralytics)
# requests              # Для загрузки весов YOLO (тянется за ultralytics)